export default class Planet {
  constructor(name, encounterId, postcardId, gameId) {
    this.name = name;
    this.encounterId = encounterId;
    this.postcardId = postcardId;
    this.gameId = gameId;
  }
}
